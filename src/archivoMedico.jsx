import { View, Text } from "react-native"
export const ArchivoMedico = () =>{
    return(
        <View style={{marginTop:50,marginLeft:20}}>
            <Text style={{fontSize:40,fontWeight:500}}>Archivo Medico</Text>
        </View>
    )
}